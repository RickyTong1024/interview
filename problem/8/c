#include <stdio.h>

char* transfer(int m, int n) {
	
}

int main(int argc, char *argv[]) {
	int m, n;
	scanf("%d %d", &m, &n);
	char* out_num = transfer(m, n);
	printf("%s\n", out_num);
	free(out_num);
	return 0;
}
